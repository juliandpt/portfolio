---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto max-w-[90%] md:max-w-[70%] lg:max-w-[50%] py-20 sm:py-32`}
>
  <slot />
</section>

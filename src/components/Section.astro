---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto max-w-[90%] md:max-w-[70%] lg:max-w-[40%] py-32`}
>
  <slot />
</section>
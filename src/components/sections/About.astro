---
import Link from '@/components/ui/Link.astro'
import { socials } from '@/consts/socials'
---

<section id="about" class="animate-in animate-delay-0 pt-30">
  <article
    class="prose dark:prose-invert prose-a:font-normal prose-a:decoration-neutral-500 prose-a:decoration-dotted prose-a:underline-offset-[5px] prose-a:opacity-80"
  >
    <p class="font-light leading-loose text-neutral-200">
      Soy <span class="font-medium tracking-tight text-white">Julián</span>, software engineer con
      +4 años de experiencia. Enfoque en escribir código limpio, optimizado y centrado en la
      experiencia del usuario, siempre buscando soluciones innovadoras para resolver problemas
      complejos.
    </p>
  </article>

  <div class="mt-10 flex items-center justify-center">
    {
      socials.map((social) => (
        <Link
          link={social.link}
          techLogo={social.techLogo}
          classes={{
            logoClass:
              `h-6 w-6 opacity-50 duration-200 fill-neutral-500 hover:scale-110 hover:opacity-100 mx-10 ${social.hover}`,
          }}
        />
        <span class="w-0.25 h-2.5 rounded-full bg-neutral-500 last:hidden"></span>
      ))
    }
  </div>
</section>
